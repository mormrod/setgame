import React from 'react';

const CardsRemaining = (props) => {
    return (
        <div>
            <span>{props.remaining} Cards Remaining</span>
        </div>
    );
};

export default CardsRemaining;